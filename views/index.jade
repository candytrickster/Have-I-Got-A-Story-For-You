extends layout

block content

    h1 Story Title
        h3 Story Content
    table(class='table')
                    tr
                        th Title
                        th Story
                        each s in stories
                            th #{s.title}
                            th #{s.content}
                    