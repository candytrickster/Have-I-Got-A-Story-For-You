extends layout

block content

    each story in stories
        h1 #{story.title}
        h3 #{story.content}
        
        
    button(class="btn", data-action="bea", onclick="location.href='story';") Add To
    button(class="btn", data-action="bea", onclick="location.href='story';") Finish
    